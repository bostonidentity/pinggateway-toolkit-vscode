{
  "name": "missing-required-route",
  "condition": "${request.uri.path == '/profile'}",
  "handler": {
    "type": "Chain",
    "config": {
      "filters": [
        {
          "type": "UserProfileFilter",
          "config": {
          }
        }
      ],
      "handler": "ClientHandler"
    }
  }
}
